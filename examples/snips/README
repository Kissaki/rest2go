A simple rest.go server and client.

Prerequirements
$ go get github.com/Kissaki/rest.go

Compile with
$ go build snips.go rest.go collection.go

Start server with
$ ./snips -server

Run client with
$ ./snips

You can also access the server with other tools or your webbrowser (which will allos you to at least Index and Find(Id) by GET requests).
Open http://127.0.0.1:3000/snips/ in your browser.

